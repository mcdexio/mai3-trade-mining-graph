type User @entity {
    # user address
    id: ID!
    stakedMCB: BigDecimal!
    unlockMCBTime: Int!
    marginAccounts: [MarginAccount!] @derivedFrom(field:"user")
}

type MarkPrice @entity {
    # pool address + "-" + perpetual index
    id: ID!
    price: BigDecimal!
    timestamp: Int!
}

type MarginAccount @entity {
    # pool address + "-" + perpetual index + "-" + user address
    id: ID!
    user: User!
    position: BigDecimal!
    totalFee: BigDecimal!
    lpFee: BigDecimal!
    vaultFee: BigDecimal!
    operatorFee: BigDecimal!
    referralRebate: BigDecimal!
    trades: [Trade!] @derivedFrom(filed:"trader")
}

type Trade @entity {
    # transactionHash
    id: ID!
    perpBlock: PerpetualTradeBlock!
    trader: MarginAccount!
    amount: BigDecimal!
    price: BigDecimal!
    fee: BigDecimal!
    lpFee: BigDecimal!
    vaultFee: BigDecimal!
    operatorFee: BigDecimal!
    referralRebate: BigDecimal!
}

type PerpetualTradeBlock @entity {
    # pool address + "-" + perpetual index + "-" + blockNumber
    id: ID!
    trades: [Trade!] @derivedFrom(field:"perpBlock")
}